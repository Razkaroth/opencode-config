---
description: Commit your changes
agent: build
model: github-copilot/gpt-5-mini
---

1. run `git status` to get currently changed files.
2. stage the files affected by our session.
3. commit using the `(kind): desc` convention, with extra details on the commit body.

IMPORTANT: Never push changes and do not include files under `reference/` under any circumstance.
